lsblk                        # List disks and partitions

fdisk -l                     # List partitions

mount /dev/sdb1 /mnt         # Mount disk

umount /mnt                  # Unmount disk

df -Th                       # Filesystem usage

mkfs.ext4 /dev/sdb1          # Format partition

blkid                         # Get UUIDs of disks
